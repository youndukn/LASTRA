class TrainSet():

    def __init__(self, input, output, reward, total_reward):
        self.input = input
        self.output = output
        self.reward = reward
        self.total_reward = total_reward
